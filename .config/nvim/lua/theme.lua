vim.cmd("hi clear")
if vim.fn.exists("syntax_on") == 1 then
    vim.cmd("syntax reset")
end

local p = {
    none = "none",
    accent = "#c9a5b5",
    secondary = "#bdb2e0",
    muted = "#d4b5a0",
    fg = "#c9c7cd",
    dimmed = "#7b7b80",
    separator = "#353539",
}

local function hl(group, opts)
    vim.api.nvim_set_hl(0, group, opts)
end

vim.cmd([[highlight StatusLine guibg=NONE]])

hl("Normal", { bg = "none", ctermbg = "none" })
hl("NormalNC", { bg = "none", ctermbg = "none" })
hl("TelescopeBorder", { bg = "none", fg = p.dimmed })
hl("NormalFloat", { bg = "none", fg = "none" })
hl("FloatBorder", { bg = "none", fg = p.dimmed })
hl("Pmenu", { bg = "none", fg = p.dimmed })
hl("PmenuSel", { bg = "none", fg = p.secondary })
hl("PmenuThumb", { bg = p.dimmed, fg = p.dimmed })
hl("PmenuSbar", { bg = p.dimmed, fg = p.dimmed })
hl("@lsp.type.struct", { bg = "none", fg = p.secondary })
hl("@lsp.type", { bg = "none", fg = p.secondary })
hl("@lsp.type.keyword", { bg = "none", fg = p.accent })
hl("@lsp.type.string", { bg = "none", fg = p.muted })
hl("@lsp.type.method", { bg = "none", fg = p.secondary })
hl("@lsp.type.function", { bg = "none", fg = p.secondary })
hl("@lsp.type.function.swift", { bg = "none", fg = p.secondary })
hl("@lsp.type.decorator", { bg = "none", fg = p.secondary })
hl("@lsp.type.modifier", { bg = p.none, fg = p.secondary })
hl("@variable", { bg = p.none, fg = p.fg })
hl("@variable.member", { bg = p.none, fg = p.fg })
hl("@variable.parameter", { bg = p.none, fg = p.fg })
hl("@type.swift", { bg = p.none, fg = p.secondary })
hl("Type", { bg = p.none, fg = p.secondary })
hl("@type.definition", { bg = p.none, fg = p.secondary })
hl("@keyword.type", { bg = p.none, fg = p.accent })
hl("@keyword.modifier", { bg = p.none, fg = p.accent })
hl("@variable.builtin", { bg = p.none, fg = p.accent })
hl("@keyword", { bg = p.none, fg = p.accent })
hl("@attribute", { bg = p.none, fg = p.secondary })
hl("@string", { bg = p.none, fg = p.muted })
hl("@constructor", { bg = p.none, fg = p.accent })
hl("@function.method", { bg = p.none, fg = p.secondary })
hl("Function", { bg = p.none, fg = p.secondary })
hl("@function.call", { bg = p.none, fg = p.secondary })
hl("SnippetTabStop", { bg = p.none, fg = p.none })
hl("DiagnosticUnnecessary", { bg = p.none, fg = p.none })
hl("@lsp.type.function.swift", { bg = p.none, fg = p.fg })
hl("@punctuation.bracket.lua", { bg = p.none, fg = p.fg })
hl("@constructor.lua", { bg = p.none, fg = p.fg })
hl("Constant", { bg = p.none, fg = p.muted })
hl("Special", { bg = p.none, fg = p.secondary })
hl("@number", { bg = p.none, fg = p.muted })
hl("@number.lua", { bg = p.none, fg = p.muted })
hl("@lsp.typemod.property.defaultLibrary.swift", { bg = p.none, fg = p.accent })
hl("@lsp.typemod.method.defaultLibrary.swift", { bg = p.none, fg = p.accent })
hl("@lsp.typemod.function.defaultLibrary.swift", { bg = p.none, fg = p.accent })
hl("@lsp.typemod.method.static.swift", { bg = p.none, fg = p.secondary })
hl("@lsp.mod.property.static.swift", { bg = p.none, fg = p.fg })
hl("@lsp.type.property", { bg = p.none, fg = p.fg })
hl("@lsp.type.class.swift", { bg = p.none, fg = p.secondary })
hl("@lsp.type.struct.swift", { bg = p.none, fg = p.secondary })
hl("@keyword.conditional.ternary.swift", { bg = p.none, fg = p.fg })
hl("@punctuation.special.swift", { bg = p.none, fg = p.fg })
hl("@function.macro.swift", { bg = p.none, fg = p.secondary })
hl("@lsp.type.enumMember.swift", { bg = p.none, fg = p.secondary })
hl("@lsp.type.enum.rust", { bg = p.none, fg = p.secondary })
hl("@lsp.type.enumMember.rust", { bg = p.none, fg = p.secondary })
hl("@lsp.type.macro.rust", { bg = p.none, fg = p.accent })
hl("@lsp.type.lifetime.rust", { bg = p.none, fg = p.muted })
hl("@lsp.type.builtinType.rust", { bg = p.none, fg = p.accent })
hl("@lsp.type.interface.rust", { bg = p.none, fg = p.secondary })
hl("@lsp.type.typeParameter.rust", { bg = p.none, fg = p.muted })
hl("@lsp.typemod.function.unsafe.rust", { bg = p.none, fg = p.secondary, italic = true })
hl("@lsp.type.comment.rust", { bg = p.none, fg = p.dimmed })
hl("DiagnosticUnderlineError", { undercurl = true, sp = p.accent })
hl("DiagnosticUnderlineWarn", { undercurl = true, sp = p.muted })
hl("DiagnosticUnderlineInfo", { undercurl = true, sp = p.secondary })
hl("DiagnosticUnderlineHint", { undercurl = true, sp = p.muted })
hl("BlinkCmpMenuSelection", { bg = p.none, fg = p.fg })
hl("CursorLine", { bg = p.none, fg = p.none })
hl("netrwdir", { bg = p.none, fg = p.secondary })
hl("DiagnosticVirtualTextInfo", { bg = p.none, fg = p.secondary })
hl("Directory", { bg = p.none, fg = p.secondary })
hl("SnacksWinSeparator", { bg = p.none, fg = p.none, sp = p.none })
hl("SnacksPickerIconMethod", { bg = p.none, fg = p.fg, sp = p.none })
hl("SnacksPickerMatch", { bg = p.none, fg = p.fg })
hl("SnacksPickerDirectory", { bg = p.none, fg = p.fg })
hl("SnacksPickerFile", { bg = p.none, fg = p.dimmed })
hl("SnacksPickerInputSearch", { bg = p.none, fg = p.fg })
hl("SnacksPickerBoxBorder", { bg = p.none, fg = p.dimmed })
hl("SnacksPickerListBorder", { bg = p.none, fg = p.dimmed })
hl("SnacksPickerInputBorder", { bg = p.none, fg = p.dimmed })
hl("LspInlayHint", { bg = p.none, fg = p.dimmed })
hl("SnacksPickerBorder", { bg = p.none, fg = p.dimmed })
hl("DiffChange", { bg = p.none, fg = p.none })
hl("DiffDelete", { bg = "#4b1818", fg = p.none })
hl("DiffText", { bg = "#597B60", fg = p.none })
hl("DiffAdd", { bg = p.none, fg = p.none })
hl("WinSeparator", { bg = p.none, fg = p.separator })
hl("SnacksPickerPreviewBorder", { bg = p.none, fg = p.dimmed })
hl("Substitute", { bg = p.accent, fg = p.separator })
hl("ColorColumn", { bg = p.none })
hl("RenderMarkdownH2Bg", { link = "Normal" })
hl("RenderMarkdownH1Bg", { link = "Normal" })
hl("RenderMarkdownH3Bg", { link = "Normal" })
hl("RenderMarkdownH4Bg", { link = "Normal" })
hl("RenderMarkdownH5Bg", { link = "Normal" })
hl("RenderMarkdownH6Bg", { link = "Normal" })
hl("CodeCompanionInlineDiffHint", { bg = p.none })
hl("TermCursor", { bg = p.accent })

-- Editor UI
hl("Visual", { bg = p.separator })
hl("Search", { bg = p.separator, fg = p.muted })
hl("IncSearch", { bg = p.accent, fg = p.separator })
hl("CurSearch", { bg = p.accent, fg = p.separator })
hl("LineNr", { bg = p.none, fg = p.separator })
hl("CursorLineNr", { bg = p.none, fg = p.dimmed })
hl("SignColumn", { bg = p.none })
hl("FoldColumn", { bg = p.none, fg = p.separator })
hl("Folded", { bg = p.none, fg = p.dimmed })
hl("MatchParen", { bg = p.separator, fg = p.fg, bold = true })
hl("NonText", { bg = p.none, fg = p.separator })
hl("EndOfBuffer", { bg = p.none, fg = p.none })
hl("Whitespace", { bg = p.none, fg = p.separator })
hl("Title", { bg = p.none, fg = p.accent, bold = true })
hl("ErrorMsg", { bg = p.none, fg = p.accent })
hl("WarningMsg", { bg = p.none, fg = p.muted })
hl("Todo", { bg = p.none, fg = p.accent, bold = true })
hl("DiagnosticError", { bg = p.none, fg = p.accent })
hl("DiagnosticWarn", { bg = p.none, fg = p.muted })
hl("DiagnosticHint", { bg = p.none, fg = p.dimmed })
hl("DiagnosticInfo", { bg = p.none, fg = p.secondary })

-- Generic treesitter groups (YAML, JSON, TOML, Markdown, etc.)
hl("@comment", { bg = p.none, fg = p.dimmed })
hl("@boolean", { bg = p.none, fg = p.accent })
hl("@operator", { bg = p.none, fg = p.fg })
hl("@punctuation", { bg = p.none, fg = p.fg })
hl("@punctuation.bracket", { bg = p.none, fg = p.fg })
hl("@punctuation.delimiter", { bg = p.none, fg = p.fg })
hl("@punctuation.special", { bg = p.none, fg = p.accent })
hl("@property", { bg = p.none, fg = p.secondary })
hl("@label", { bg = p.none, fg = p.secondary })
hl("@tag", { bg = p.none, fg = p.accent })
hl("@tag.attribute", { bg = p.none, fg = p.secondary })
hl("@tag.delimiter", { bg = p.none, fg = p.fg })
hl("@type", { bg = p.none, fg = p.secondary })
hl("@type.builtin", { bg = p.none, fg = p.accent })
hl("@module", { bg = p.none, fg = p.secondary })
hl("@string.escape", { bg = p.none, fg = p.accent })
hl("@string.special", { bg = p.none, fg = p.muted })
hl("@string.special.url", { bg = p.none, fg = p.secondary, underline = true })
hl("@constant.builtin", { bg = p.none, fg = p.accent })
hl("@function.builtin", { bg = p.none, fg = p.secondary })
hl("@markup.heading", { bg = p.none, fg = p.accent, bold = true })
hl("@markup.bold", { bold = true })
hl("@markup.italic", { italic = true })
hl("@markup.link", { bg = p.none, fg = p.secondary, underline = true })
hl("@markup.raw", { bg = p.none, fg = p.muted })
hl("@markup.list", { bg = p.none, fg = p.accent })

-- Cmdline syntax highlighting (ui2)
hl("@keyword.vim", { bg = p.none, fg = p.accent })
hl("@function.call.vim", { bg = p.none, fg = p.secondary })
hl("@function.macro.vim", { bg = p.none, fg = p.secondary })
hl("@string.vim", { bg = p.none, fg = p.muted })
hl("@number.vim", { bg = p.none, fg = p.muted })
hl("@variable.vim", { bg = p.none, fg = p.fg })
hl("@variable.builtin.vim", { bg = p.none, fg = p.accent })
hl("@type.vim", { bg = p.none, fg = p.secondary })
hl("@operator.vim", { bg = p.none, fg = p.fg })
hl("@string.special.path.vim", { bg = p.none, fg = p.muted })
hl("@module.vim", { bg = p.none, fg = p.dimmed })
hl("@comment.vim", { bg = p.none, fg = p.dimmed })
